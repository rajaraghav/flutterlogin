import 'package:flutter/material.dart';

class Login extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    var app = new Card(
      child: new Container(
        color: new Color('#42A5F5'),
      ),
      elevation: 5.0,
    );
    return app;
  }
}
